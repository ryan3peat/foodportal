# ===========================================
# Essential Flavours Supplier Portal
# Environment Variables Template
# ===========================================

# ----- BASE URL CONFIGURATION -----
# CRITICAL: Set this to your production deployment URL
# Example: https://your-app-name.replit.app
# This is used for generating email links (magic links, quote submission URLs, etc.)
BASE_URL=

# Note: REPLIT_DEV_DOMAIN is automatically set by Replit in development mode
# You don't need to set it manually

# ----- DATABASE CONFIGURATION -----
# PostgreSQL connection string
# Automatically set by Replit when you provision a database
DATABASE_URL=

# Individual database credentials (auto-set by Replit)
PGHOST=
PGPORT=
PGUSER=
PGPASSWORD=
PGDATABASE=

# ----- SESSION CONFIGURATION -----
# Secret key for session encryption (use a strong random string)
# Generate with: openssl rand -base64 32
SESSION_SECRET=

# ----- EMAIL SERVICE CONFIGURATION -----
# Email provider: 'graph' (Microsoft Graph) or 'mock' (development)
EMAIL_PROVIDER=graph

# Allow fallback to mock email service if primary provider fails
# Set to 'true' in development, 'false' in production
ALLOW_MOCK_FALLBACK=false

# ----- MICROSOFT GRAPH API CREDENTIALS -----
# Required for sending emails via Microsoft 365
# Get these from Azure Portal: https://portal.azure.com
AZURE_TENANT_ID=
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
SENDER_EMAIL=

# ----- SERVER CONFIGURATION -----
# Port number (automatically set by Replit to 5000)
PORT=5000

# Node environment (development or production)
NODE_ENV=development
